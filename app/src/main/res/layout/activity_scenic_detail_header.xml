<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="55dp"
              android:orientation="vertical"
    >

    <LinearLayout
        android:padding="@dimen/d_10"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <Button
            android:onClick="doBack"
            android:drawablePadding="@dimen/d_5"
            android:background="@null"
            android:textSize="20sp"
            android:textColor="@color/white"
            android:id="@+id/tv_detail_scenic_name"
            android:text="@string/jiulong"
            android:drawableLeft="@mipmap/arrow1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <TextView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"/>

        <ImageView
            android:id="@+id/img_isfavorite"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:src="@mipmap/detail_shou"
            android:onClick="onCollect"
            />
    </LinearLayout>
</LinearLayout>